# status: "tested"
# last_tested_version: "0.8.5"
# platform_notes: "Tested on Proxmox LXC: docker (0.8.5, 2026-09-14), podman (0.8.5, 2026-09-14); Proxmox VM: docker (0.8.5, 2026-09-14), podman (0.8.5, 2026-09-14)."
# breaking_changes: none
services:
  it-tools:
    container_name: njorddeploy-it-tools
    image: "{{ image_name }}:{{ component_version }}"
    ports:
      - "{{ IT_TOOLS_WEB_PORT }}:80"
    restart: unless-stopped
    networks:
      - njorddeploy_net

networks:
  njorddeploy_net:
    external: true
