# status: "tested"
# last_tested_version: "4.0.19.2979-ls321"
# platform_notes: "Tested on Proxmox LXC: docker (4.0.19.2979-ls321, 2026-08-15), podman (4.0.19.2979-ls321, 2026-08-15); Proxmox VM: docker (4.0.19.2979-ls321, 2026-08-15), podman (4.0.19.2979-ls321, 2026-08-15)."
# breaking_changes: "None"
services:
  sonarr:
    image: lscr.io/linuxserver/sonarr:{{ component_version }}
    container_name: njorddeploy-sonarr
    restart: unless-stopped
    environment:
      - "PUID={{ PUID | default('1000') }}"
      - "PGID={{ PGID | default('1000') }}"
      - "TZ={{ TZ | default('Europe/Amsterdam') }}"
    volumes:
      - "{{ CONFIG_BASE_PATH }}/sonarr/config:/config"
      # Point this to your TV shows library, should be the same as Jellyfin's TV folder
      - "{{ CONFIG_BASE_PATH }}/jellyfin/tvshows:/tv"
      # Point this to where your download client saves completed files
      - "{{ CONFIG_BASE_PATH }}/downloads:/downloads"
    ports:
      - "{{ SONARR_WEB_PORT | default('8989') }}:8989"
    networks:
      - njorddeploy_net

networks:
  njorddeploy_net:
    external: true
