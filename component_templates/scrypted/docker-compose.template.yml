# status: "tested"
# last_tested_version: "24.04"
# platform_notes: "Tested on Proxmox LXC: docker (24.04, 2026-08-15), podman (24.04, 2026-08-15); Proxmox VM: docker (24.04, 2026-08-15), podman (24.04, 2026-08-15)."
# breaking_changes: "None"
services:
  scrypted:
    container_name: scrypted
    image: koush/scrypted:{{ component_version }}
    restart: unless-stopped
    ports:
      - "{{ PORT_SCRYPTED | default(10443) }}:10443"
    volumes:
      - "{{ CONFIG_BASE_PATH }}/scrypted:/server/volume"
    networks:
      - njorddeploy_net

networks:
  njorddeploy_net:
    external: true
