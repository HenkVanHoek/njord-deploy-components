# status: "tested"
# last_tested_version: "latest"
# platform_notes: "Tested successfully on Proxmox LXC."
# breaking_changes: "None"
services:
  organizr:
    container_name: njorddeploy-organizr
    image: organizr/organizr:{{ component_version }}
    restart: unless-stopped
    ports:
      - "{{ PORT_ORGANIZR | default(8095) }}:80"
    volumes:
      - "{{DATA_ROOT}}/organizr/config:/config"
    environment:
      - "PUID={{PUID}}"
      - "PGID={{PGID}}"
      - "TZ={{TZ}}"
    networks:
      - njorddeploy_net

networks:
  njorddeploy_net:
    external: true
