# status: "tested"
# last_tested_version: "1.31.3"
# platform_notes: "Tested on Proxmox LXC: docker (1.31.3, 2026-08-15); Proxmox VM: docker (1.31.3, 2026-08-15)."
# breaking_changes: "None"
services:
  fluffychat-web:
    container_name: njorddeploy-fluffychat-web
    build:
      context: https://github.com/HenkVanHoek/pish-fluffychat-web.git#main
      dockerfile: Dockerfile
    image: {{ image_name }}:{{ component_version }}
    pull_policy: build
    ports:
      - "{{ FLUFFYCHAT_WEB_PORT }}:80"
    environment:
      - "CHAT_MODE={{ CHAT_MODE | default('your-own-domain') }}"
      - "HOMESERVER_DOMAIN={{ HOMESERVER_DOMAIN | default('matrix.org') }}"
    restart: unless-stopped
    networks:
      - njorddeploy_net

networks:
  njorddeploy_net:
    external: true
